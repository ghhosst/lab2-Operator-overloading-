ѕри перегрузке операторов с помощью метода будет следующий синтаксис написани€
заголовков:

// префикс Ц возвращает после инкремента
	Vector& operator ++ / -- ();

// постфикс Ц возвращает значение ƒќ инкремента
	Vector operator ++ / -- (int);




ѕри перегрузке операторов с помощью дружественных функций синтаксис написани€
заголовков будет следующий:

// префикс Ц возвращает после инкремента
	const Vector& operator ++(--)(Vector& v)

// постфикс Ц возвращает значение ƒќ инкремента
	const Vector operator ++(--) (Vector &v, int);



когда мы пишем конструктор, который принимает только одно значение, то мы пишем по сути оператор преобразовани€.